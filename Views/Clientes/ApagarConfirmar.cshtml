@model ClientesModel
@{
    ViewData["Title"] = "Confirmar o apagar do Cliente";
}


<div class="text-center">
    <h1 class="display-5">Apagar cliente</h1>
</div>
<br />
<div>
    <h3>Tem certeza que deseja apagar o cliente (@Model.Nome) ? </h3>
</div>
<br />
<a class="btn btn-primary"asp-controller="Clientes" asp-action="Apagar" asp-route-id="@Model.Id" role="button">Confirmar exclusão</a>
<a class="btn btn-secondary" role="button" asp-controller="Clientes" asp-action="Index">Cancelar exclusão</a>